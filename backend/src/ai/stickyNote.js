export const generateStickyNote = async (prompt) => {
    // Placeholder logic for generating a clean sticky note content
    console.log(`Generating sticky note for prompt: ${prompt}`);

    // Simulating a cleaned-up/summarized version of the prompt suitable for a sticky note
    const cleanedText = `Points from "${prompt}":\n- Key Idea 1\n- Key Idea 2\n- Action Item`;

    return cleanedText;
};
